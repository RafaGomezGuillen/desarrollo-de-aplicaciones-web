@model ErrorViewModel
@{
    ViewData["Title"] = "Error";
}

<h1 class="text-danger">Página de Error.</h1>
<h2 class="text-danger">Se ha producido un error al procesar la solicitud.</h2>

@if (Model.ShowRequestId)
{
    <p>
        <strong>ID solicitado:</strong> <code>@Model.RequestId</code>
    </p>
}

<h3>Modo de desarrollo</h3>
<p>
    Al cambiar al entorno <strong>de desarrollo</strong>, se mostrará información más detallada sobre el error que se ha producido.
</p>
<p>
    <strong>El entorno de desarrollo no debe estar habilitado para las aplicaciones implementadas.</strong>
    Puede dar lugar a la visualización de información confidencial de excepciones a usuarios finales.
    Para la depuración local, habilite el entorno de <strong>desarrollo</strong> estableciendo la variable <strong>de entorno ASPNETCORE_ENVIRONMENT</strong> en <strong>Desarrollo</strong>
    y reiniciar la aplicación.
</p>
